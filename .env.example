# Web app (Vite) — add these in Vercel project settings when Supabase is wired up (Phase 3)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Server-only (Phase 9) — never prefix with VITE_
# OPENAI_API_KEY=sk-...
