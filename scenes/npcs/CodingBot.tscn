[gd_scene load_steps=5 format=3 uid="uid://rw_codingbot"]

[ext_resource type="Script" path="res://scripts/npcs/CodingBot.gd" id="1_codingbot"]

[sub_resource type="CapsuleShape2D" id="CapsuleShape2D_1"]
radius = 12.0
height = 32.0

[sub_resource type="CircleShape2D" id="CircleShape2D_1"]
radius = 50.0

[sub_resource type="PlaceholderTexture2D" id="PlaceholderTexture2D_1"]
size = Vector2(36, 52)

; ──────────────────────────────────────────────────────────────
; Coding Bot NPC
; StaticBody2D — doesn't move, blocks the player, can be talked to.
; ──────────────────────────────────────────────────────────────

[node name="CodingBot" type="StaticBody2D"]
collision_layer = 4
collision_mask = 0
script = ExtResource("1_codingbot")

; Body collision — player bumps into the NPC instead of walking through.
[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(0, 4)
shape = SubResource("CapsuleShape2D_1")

; Placeholder sprite (light-blue tinted). Replace with real art later!
[node name="Sprite2D" type="Sprite2D" parent="."]
texture = SubResource("PlaceholderTexture2D_1")
modulate = Color(0.4, 0.8, 1.0, 1)

; Name tag displayed above the NPC.
[node name="NameLabel" type="Label" parent="."]
position = Vector2(-50, -56)
custom_minimum_size = Vector2(100, 0)
text = "Coding Bot"
horizontal_alignment = 1
theme_override_font_sizes/font_size = 12
modulate = Color(0.8, 1.0, 1.0, 1)

; Question mark badge — shows the NPC has a mission available.
[node name="QuestMarker" type="Label" parent="."]
position = Vector2(-6, -72)
text = "❓"
theme_override_font_sizes/font_size = 20

; Interact area — lets the player's InteractArea detect this NPC.
[node name="InteractArea" type="Area2D" parent="."]
collision_layer = 0
collision_mask = 0
monitoring = true
monitorable = true

[node name="InteractCollision" type="CollisionShape2D" parent="InteractArea"]
shape = SubResource("CircleShape2D_1")
